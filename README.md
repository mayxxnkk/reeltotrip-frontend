# 🚀 Project Setup Guide

Follow the steps below to run this project locally on your system.

## 📥 1. Clone the Repository

```
git clone <your-repository-url>
cd <project-folder-name>
```

Or simply download and extract the ZIP file, then open the project folder.

## 📦 2. Install Dependencies

Make sure you have Node.js installed. Then run:

```
npm install
```

This will install all required dependencies listed in package.json.

## ▶️ 3. Run the Development Server

Start the development server using:

```
npm run dev
```

After running this command, the app will start locally.

Open your browser and visit the link.
